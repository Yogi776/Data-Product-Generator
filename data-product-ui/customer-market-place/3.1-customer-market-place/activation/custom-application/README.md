# Custom Application for npa-early-warning

This directory contains custom applications for consuming npa-early-warning data.

## Structure
- `applications/` - Custom application code
- `config/` - Application configuration files
- `docs/` - Application documentation

## Usage
Custom applications can be developed here to consume the npa-early-warning data product through various interfaces.
